@{
    ViewBag.Title = "Settings";
}
<div class="container">
    <h2>Settings</h2>
    <div class="row" style="height: auto !important">
        <div class="col-md-4"><br/>
            @using (Ajax.BeginForm("ShowApprDocsType", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="ApprDocsTypes" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr/>
            @using (Ajax.BeginForm("ShowStrength", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="Strength" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr/>
            @using (Ajax.BeginForm("ShowManufacturer", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="Manufacturer" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr />
            @using (Ajax.BeginForm("ShowArtwork", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="Artwork" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr />
            @using (Ajax.BeginForm("ShowMarketingAuthorizHolder", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="MarketingAuthorizHolder" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr />
            @using (Ajax.BeginForm("ShowPharmaceuticalForm", new AjaxOptions { UpdateTargetId = "results" }))
            {
                <p>
                    <input type="submit" class="success" value="PharmaceuticalForm" />
                    <span class="glyphicon glyphicon-menu-right"></span>
                </p>
            }
            <hr/>
        </div>
        <div class="col-md-8">
            <div id="results"></div>
        </div>
    </div>
</div><div class="empty"></div>
@Scripts.Render("~/bundles/Ajax")
@*<script src="@Url.Content("~/Scripts/jquery-3.3.1.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Scripts/jquery.unobtrusive-ajax.min.js")" type="text/javascript"></script>*@




