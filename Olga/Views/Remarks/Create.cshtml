@model Olga.Models.RemarkViewModel
@{
    ViewBag.Title = "Create";
}
<div class="container">
    <h2>Create Remark</h2>
    <div class="row" style="overflow: auto;">
        @using (Html.BeginForm("Create", "Remarks", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" }))
    {
        @Html.AntiForgeryToken()
        <div class="form-horizontal">
            @Html.HiddenFor(model => model.Id)
            
            @*<div class="form-group">
                @Html.LabelFor(model => model.ProductName, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.DropDownListFor(model => model.ProductNameId, new SelectList(@ViewBag.ProductNames, "Id", "Name"), new { @class = "form-control" })
                </div>
            </div>*@
            
            <div class="form-group">
                @Html.LabelFor(model => model.Comments, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.TextBoxFor(model => model.Comments, new { @class = "form-control" })
                </div>
            </div>

        </div>
            }
        </div>
    </div>
<div class="empty"></div>

