1. create aap in fb https://developers.facebook.com/apps/
2. fill the items, make app alive
3. for only one user 

   - get code https://developers.facebook.com/apps/:

     variant 1:

                  https://www.facebook.com/dialog/oauth/?client_id=1001622006550427&redirect_uri=http://nrk.com.ua/Admin/blocks/fb.php&response_type=code&scope=public_profile,      publish_actions,user_friends,user_posts,user_photos,email,user_birthday,user_location&grant_type=client_credentials
     variant 2: 
                   1. create file fb.php                   
                   2. put in your handler:
                    require_once 'soc_config.php';
                    if (isset($_GET['code']){
                    $code =  $_GET['code'];   
                    $token_url = "https://graph.facebook.com/oauth/access_token?client_id="
                             . $fb_app_id . "&redirect_uri=" . urlencode($my_url.'fb.php') 
                             . "&client_secret=" . $fb_app_secret 
                             . "&code=" . $code;
                    $access_token = substr(file_get_contents($token_url),13,-16); 
                    echo $access_token;
   }
   - 
